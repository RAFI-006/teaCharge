<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teashop.teacharge">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.example.USB_PERMISSION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_login_primg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.LaunchScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.LoginActivity" />
        />
        <activity android:name=".View.ProductsCategory" />
        <activity android:name=".View.ItemListActivity" />
        <activity android:name=".View.ProductsDescription" />
        <activity
            android:name=".View.GenerateBill"
            android:label="@string/title_activity_generate_bill"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".View.TransactionHistory"
            android:label="@string/title_activity_transaction_history"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Demo"
            android:label="@string/title_activity_demo"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>